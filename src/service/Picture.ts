/**
 * representation of a Picture entry in the database
 */
interface Picture{
    /**
     * identifier of a picture
     */
    id: any;
    /**
     * relative path to the picture file's location
     */
    path: string;
}